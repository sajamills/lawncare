## Codebase Patterns
- Next.js 15 project using App Router with `src/` directory layout (`src/app/`, `src/lib/`, `src/data/`, `src/actions/`)
- Tailwind CSS v4 uses `@import "tailwindcss"` in globals.css (not `@tailwind` directives)
- Tailwind CSS v4 custom colors: define via `@theme { --color-<name>: <value>; }` in globals.css; no tailwind.config.ts needed
- Use `var(--color-<name>)` in inline styles for semantic color tokens defined in `@theme`
- TypeScript strict mode enabled via tsconfig.json
- .gitignore blocks `.env*` — commit example env files as `env.local.example` (no leading dot)
- Clerk auth: @clerk/nextjs package; server actions use auth() from @clerk/nextjs/server; client components use useUser()/useClerk() hooks; ClerkProvider wraps layout
- Onboarding state persisted in sessionStorage under key `onboarding_state`; session UUID in localStorage under `lawn_session_id`
- All onboarding step components take `{ onNext: () => void }` prop; routing handled by `src/app/onboarding/[step]/page.tsx`
- DB uses `@vercel/postgres` (createPool); works with any `POSTGRES_URL` env var; createPool() is LAZY (only called on first query) to avoid module-load crash in dev without POSTGRES_URL
- All DB migrations use IF NOT EXISTS — safe to re-run
- grassTypes data in `src/data/grass-types.ts`; each entry has id, name, description, traits[], photoUrl, photoCredit, bladeWidth, texture, activeSeason
- Pre-emergent windows in `src/data/preemergent-windows.ts`; getPreemergentWindow(state) returns spring/fall date ranges
- `/api/parse-pdf` route: accepts { state, grassType, pdfUrl? }; checks DB cache first; calls Claude claude-sonnet-4-6; returns WeeklyPlan[] (52 entries); WeeklyPlan and WeeklyTask types exported from that route file
- ISO week utility getISOWeek(date): uses UTC math; week 1 = week containing Jan 4. June 15 2026 = ISO week 25 (verified). Now lives in src/lib/week-utils.ts (shared between dashboard and tests)
- Shared week/plan utilities in src/lib/week-utils.ts: getISOWeek, getWeekDateRange, normalizeGrassId, normalizeStateCode, getWeekPlan, findNextTaskWeek
- Test fixtures in __tests__/fixtures/ — use deterministic JSON, never call Claude/external APIs from tests
- vitest installed; npm test runs __tests__/plan-utils.test.ts (16 tests)
- grassType and state MUST be normalized before cache lookup: normalizeGrassId (lowercase+trim), normalizeStateCode (uppercase+trim)
- Pexels CDN URL format: https://images.pexels.com/photos/[ID]/pexels-photo-[ID].jpeg?auto=compress&cs=tinysrgb&w=640 — ALWAYS verify photo ID returns 200 before using
- next.config.ts has PWA wrapper in prod — add config changes to base nextConfig object before the PWA wrap
- Bracket paths require quoting in git add: git add "src/app/onboarding/[step]/..."
- Layout: src/app/layout.tsx is a SERVER component (exports metadata); client nav is in src/app/ClientNav.tsx ("use client", usePathname, nav rendering)
- Design tokens in globals.css @theme: --color-background, --color-surface, --color-primary, --color-secondary, --color-text-primary, --color-text-muted, --color-surface-alt, --color-border, --color-warning-*, --color-urgent, --color-routine, --color-optional
- Hardcoded hex colors to AVOID (use CSS vars instead): #2d4a2d → var(--color-border), #3d3000 → var(--color-warning-bg), #eab308 → var(--color-warning-text), #1a3a1a → var(--color-surface-alt)
- SVG icon components: src/components/icons.tsx exports HomeIcon, CalendarIcon, ProfileIcon
- Always use borderWidth + borderStyle as separate inline style props when you need consistent border width regardless of selection state (avoids layout shift)
- Stripe webhooks: MUST use request.text() for raw body (NOT request.json()) before calling stripe.webhooks.constructEvent()
- Vercel Blob: requires BLOB_READ_WRITE_TOKEN; use put(pathname, body, { access: 'public' }) from @vercel/blob
- Claude Vision: messages array includes { type: 'image', source: { type: 'url', url: imageUrl } } image block; model claude-sonnet-4-6
- Google Maps: use @googlemaps/js-api-loader Loader singleton; libraries: ['places', 'drawing', 'geometry']; mapTypeId: 'satellite' with tilt: 0 for flat satellite view
- Deploy after EVERY story: run `vercel --yes` and verify on production URL before marking passes: true
- Step5 in onboarding is imported from Step5New.tsx (NOT Step5.tsx) — page.tsx imports Step5 from './steps/Step5' but that file re-exports or is the same as Step5New
- Onboarding has 7 steps total (Step1–Step7); Step6 is the extension resources step targeted for removal in US-013
- Treatment history is in Step5 (imported from steps/Step5.tsx, which loads the TASKS list); Step5New.tsx is the actual implementation used

# Ralph Progress Log
Started: Sun Jun 15 2026 — UI/UX Remediation Sprint
---

## 2026-06-15 - US-001
- Added isValidZip() helper (5-digit numeric check) and canSave derived boolean
- Added touchedZip + touchedGrass state to show errors only after field interaction
- ZIP input: added inputMode="numeric" autoComplete="postal-code", red border + error message when touched and invalid
- Grass type select: red border + error message when touched and empty
- Save button: disabled when !canSave, adds cursor-not-allowed
- Files changed: src/app/profile/page.tsx
- **Learnings for future iterations:**
  - Profile page uses getSessionId() from localStorage for DB queries — Clerk user_id migration is in platform-features PRD (US-060), not yet done
  - canSave must be derived at render time (not inside handleSave) so button disabled state is reactive
---

## 2026-06-15 - US-002
- Root causes diagnosed (multiple, all fixed):
  1. SYSTEM_PROMPT said "week 1 = first week of January" without ISO 8601 spec — Claude's week numbering could drift from getISOWeek(). Fixed: SYSTEM_PROMPT now explicitly specifies ISO 8601 week numbering
  2. Claude returning '[]' was silently treated as success (valid JSON, passes JSON.parse) — Fixed: added post-parse check that throws if plan.length === 0
  3. grassType not normalized before cache lookup — 'Bermudagrass' missed cache keyed as 'bermudagrass'. Fixed: normalizeGrassId() applied before all lookups
  4. Dashboard swallowed fetch errors silently with catch{}; !res.ok path never set error state. Fixed: planError state + error card UI
  5. Quiet-week state showed generic copy not derived from plan data. Fixed: findNextTaskWeek() finds real next task
- Files changed: src/app/api/parse-pdf/route.ts, src/app/dashboard/page.tsx
- New files: src/lib/week-utils.ts, __tests__/fixtures/ar-bermudagrass-plan.json, __tests__/plan-utils.test.ts, vitest.config.ts
- **Learnings for future iterations:**
  - getISOWeek(June 15 2026) = 25 (verified by UTC math and by test)
  - Empty array [] from LLM is a common failure mode — always check plan.length > 0 after JSON.parse
  - Dashboard retryCount state pattern: increment to re-run useEffect without page refresh
  - All plan utility functions (getISOWeek, findNextTaskWeek, etc.) live in src/lib/week-utils.ts — import from there, not dashboard.tsx

## 2026-06-15 - US-002 patch
- validateWeeklyPlan() added to week-utils.ts — used for BOTH cached plans and new Claude responses; rejects empty array, invalid fields, duplicate weeks, all-empty-tasks plan
- extractPlanFromText() centralizes Claude response parsing + validation; returns null on any failure
- Week 53 policy: clampWeek(53) === 52; documented in week-utils.ts; dashboard uses clampWeek(getISOWeek(now))
- WeeklyPlan/WeeklyTask types moved to week-utils.ts; route.ts re-exports for backward compat
- Retry UX: prior valid tasks preserved during retry; retrying indicator shown inline; retryFailed notification is non-destructive dismissible banner
- Client-side console.error removed from dashboard; all stage logging server-side only
- 47 tests across plan-utils.test.ts and api-plan-processing.test.ts (no live services)
---

## 2026-06-16 - US-003
- Replaced overflow-x-auto + flex gap-2 min-w-max calendar strip with a responsive CSS grid: grid grid-cols-3 sm:grid-cols-4 md:grid-cols-6 lg:grid-cols-12 gap-2
- Removed fixed w-28 shrink-0 from each month card; added w-full to outer div and button; added minHeight: 80px to the month button for breathing room
- Category dot legend below the calendar left unchanged
- Files changed: src/app/dashboard/calendar/page.tsx
- **Learnings for future iterations:**
  - No browser automation tool (no MCP playwright/puppeteer, no dev-browser skill) is wired up in this environment — UI stories can only be verified via typecheck/lint/test + manual code review unless the user adds one. Note this explicitly in progress reports rather than claiming visual verification was done.
  - /dashboard/* routes are gated by Clerk middleware (proxy.ts) — curl without a session returns 404, not the page content. This is expected auth behavior, not a bug.
  - Separately fixed (outside PRD scope): .env.local had literal placeholder Clerk key values causing a "Publishable key not valid" runtime error. Replaced with real keys provided by the user and restarted the dev server.
---

## 2026-06-16 - US-003 correction
- Reopened: lg:grid-cols-12 made month cards too narrow for the target audience; no browser-level verification existed.
- Grid changed to grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-3 (no 12-up row at any breakpoint)
- Month card: confirmed w-full, no fixed width, break-words for text wrap, minHeight raised to 96px, added focus-visible ring (focus-visible:ring-2 ring-offset-2 ring-[color:var(--color-primary)])
- Current/selected state no longer color-only: current month shows a "Now" text label; expanded/selected month shows a ▾ icon and a surface-alt background tint
- Added @playwright/test; e2e/calendar.spec.ts tests all 7 required viewports (320x800, 375x812, 414x896, 768x1024, 1024x768, 1440x900, 1920x1080): asserts document and grid scrollWidth <= clientWidth, Jan/Dec cards present and within viewport, zero .overflow-x-auto elements, list/calendar toggle still works. 7/7 pass.
- Screenshots captured at 375/768/1440px in e2e/screenshots/ (gitignored, not committed)
- playwright.config.ts runs its own dev server on port 3100 (separate from any manually-running dev server on 3000) with PLAYWRIGHT_TEST_BYPASS_AUTH=1
- src/middleware.ts: added a narrow, non-production-only bypass gated by PLAYWRIGHT_TEST_BYPASS_AUTH so Playwright can render protected /dashboard/* routes without real Clerk test-user credentials. Hard-disabled when NODE_ENV==="production".
- vitest.config.ts: added exclude: ["**/node_modules/**", "**/e2e/**"] — otherwise Vitest tries to collect Playwright spec files and errors on async test.describe()
- Reworded env.local.example placeholders to your-clerk-publishable-key/your-clerk-secret-key so the literal Clerk test-key-prefix grep checks return zero tracked-file matches, per security check. Confirmed .env.local stays gitignored and the real Clerk key values are not present in any tracked file.
- Files changed: src/app/dashboard/calendar/page.tsx, src/middleware.ts, vitest.config.ts, env.local.example, .gitignore, package.json
- New files: playwright.config.ts, e2e/calendar.spec.ts
- **Learnings for future iterations:**
  - When adding Playwright alongside Vitest, always add `exclude: ["**/e2e/**"]` to vitest.config.ts immediately — Vitest will otherwise try to run .spec.ts Playwright files and fail with a cryptic async-describe error
  - Next.js refuses to start a second dev server even on a different port if one is already running for the same project dir ("Another next dev server is already running") — kill any manually-started dev server before running `npx playwright test`, or it will fail to boot its own webServer
  - clerkMiddleware's auth.protect() returns 404 (not a redirect) for non-navigational requests like curl without browser headers — a real browser/Playwright gets the expected redirect-to-sign-in behavior instead
  - For E2E-testing routes behind Clerk without provisioning a real test user, a narrow env-var-gated bypass in middleware.ts (disabled outside test runs and always disabled in production) is the pragmatic choice over standing up full Clerk test auth
  - Visual/contrast bugs in Clerk's hosted SignIn/SignUp UI are controlled via Clerk's `appearance` prop (variables, elements, layout) passed to <ClerkProvider> or the SignIn/SignUp components — avoid brittle global CSS overrides targeting generated Clerk class names
---

## 2026-06-16 - US-CLERK-CSS
- Fixed low-contrast/unreadable Clerk SignIn and SignUp UI on the dark green LawnGuide theme.
- Root cause: sign-in/sign-up pages only overrode `colorBackground`/`colorPrimary` in Clerk's `appearance.variables`; every other variable (heading/label/divider/footer text colors) stayed at Clerk's light-theme defaults, rendering near-black text on the dark green card.
- My first attempt at writing the fix used Clerk variable/element names recalled from training data (e.g. `colorInputText`, `colorTextSecondary`, `colorTextOnPrimaryBackground`) — these do NOT exist on the installed @clerk/react version and failed typecheck with TS2561. Rather than guess-and-check against TS error messages one property at a time, wrote a one-off TypeScript Compiler API script (`ts.createProgram` + `checker.getTypeFromTypeNode` + `checker.getPropertiesOfType`) to authoritatively enumerate the real `variables` and `elements` shapes from the installed type declarations. Deleted the script after use (not committed).
- Verified real `variables` shape (24 props): colorPrimary, colorPrimaryForeground, colorBackground, colorForeground, colorMuted, colorMutedForeground, colorInput, colorInputForeground, colorBorder, colorRing, colorDanger, colorWarning, colorSuccess, colorNeutral, colorShadow, colorModalBackdrop, colorShimmer, fontFamily, fontFamilyButtons, fontFamilyMono, fontSize, fontWeight, borderRadius, spacing.
- Verified real `elements` keys by grepping the actual `.d.mts` type declaration (`node_modules/@clerk/react/dist/types-nJoQvbqq.d.mts`, since Clerk ships `.d.mts`/`.d.cts`, not plain `.d.ts` — a plain `--include="*.d.ts"` grep finds nothing). All originally-guessed element keys (`card`, `headerTitle`, `headerSubtitle`, `socialButtonsBlockButton(+Text)`, `dividerLine`, `dividerText`, `formFieldLabel`, `formFieldInput`, `formFieldHintText/ErrorText/SuccessText/Action`, `formButtonPrimary`, `footerActionText/Link`, `identityPreviewText/EditButton`, `otpCodeFieldInput`, `badge`, `alertText`, `formResendCodeLink`) turned out to be valid — only the `variables` names needed correcting.
- New file `src/lib/clerk-appearance.ts` exports a single shared `clerkAppearance` object (typed as `ComponentProps<typeof SignIn>["appearance"]`), consumed identically by both `/sign-in` and `/sign-up` pages, mapping every relevant Clerk variable/element to existing LawnGuide `--color-*` design tokens. Uses only Clerk's documented `appearance` API — no global CSS, no selectors against generated `cl-*` class names.
- Verified globals.css has no stray overlay/opacity/mix-blend rule affecting the Clerk card — confirmed the bug was purely missing variable overrides, not inherited CSS.
- Added `e2e/clerk-auth.spec.ts`: 14 Playwright tests across `/sign-in` and `/sign-up` — no-horizontal-overflow at 320/375/414/768/1440px (10 tests), card visibility+non-transparent background (2 tests), primary input keyboard-focusable (2 tests). All 14 passed. Screenshots captured at 375/768/1440px for both routes into `e2e/screenshots/` (gitignored).
- Visually reviewed the 1440/768/375px screenshots directly: headings, subtitle, Google button + label, divider line/text, email/password inputs + placeholders, primary Continue button, footer "Sign up"/"Sign in" links, and the Development mode badge are all clearly readable against the dark green card at every captured width, with no overlay/translucency artifacts.
- Scope gap (documented in prd.json notes, not silently skipped): did not exercise/screenshot the deeper multi-step flow screens (email OTP verification, password reset, account-switcher) since they need a live Clerk test user + email delivery, which this environment doesn't have. Since the fix is a shared appearance config (not per-screen overrides), those screens reuse the same `formFieldInput`/`formButtonPrimary`/`card`/etc. elements and should inherit the fix, but a manual spot-check is still recommended.
- Files changed: src/lib/clerk-appearance.ts (new), src/app/sign-in/[[...sign-in]]/page.tsx, src/app/sign-up/[[...sign-up]]/page.tsx, e2e/clerk-auth.spec.ts (new), prd.json
- **Learnings for future iterations:**
  - Do not trust training-data memory of third-party library theming/appearance API property names when they fail typecheck — write a short TypeScript Compiler API introspection script against the actually-installed `node_modules` type declarations instead of guessing property-by-property from compiler error messages. Must run the script from the project root (not /tmp) so `node_modules` resolves, and use `checker.getTypeFromTypeNode(node.type)` on an explicitly-typed variable declaration (not `getTypeAtLocation`) to get the real annotated type.
  - Clerk's type declarations for `@clerk/react` ship as `.d.mts`/`.d.cts`, not `.d.ts` — broaden grep globs accordingly when hunting for type definitions in node_modules.
  - One shared `clerkAppearance` object exported from `src/lib/clerk-appearance.ts` and imported by both SignIn/SignUp pages keeps the two forms visually consistent and avoids duplicating the (long) variables/elements config.
  - Running the full Playwright suite (calendar.spec.ts + clerk-auth.spec.ts, 21 tests) with the default 4 workers intermittently hit Clerk's dev-instance API rate limit ("too_many_requests"), failing only the later /sign-up tests in that worker batch — not a bug in the appearance fix. Mitigated in playwright.config.ts with `workers: 2` and `retries: 1`; reran 3x afterward with 21/21 passing each time.
---

## 2026-06-16 - US-004
- Replaced the empty-state dash/text in the calendar's three no-task render paths with more descriptive copy.
- Calendar-view month card: when categories.length === 0, now shows "Routine care only" instead of "—".
- MonthTaskList (expanded month panel) and MonthTaskListFlat (list view): "No tasks this month." replaced with "No seasonal treatments this month. Continue regular mowing and watering." in both.
- Files changed: src/app/dashboard/calendar/page.tsx
- **Learnings for future iterations:**
  - Per updated workflow guidance: during implementation, run only the scoped test/lint/typecheck relevant to the changed file(s); run the full vitest/lint/tsc/playwright suite once at the end of the story, not after every edit. For pure copy/text changes with no layout/responsive/color/typography impact, skip capturing new screenshots.
  - Do not reinstall or reconfigure Playwright/Vitest infra per story — reuse what already exists (e2e/calendar.spec.ts, playwright.config.ts) unless the story needs new capability.
---

## 2026-06-16 - US-005
- viewMode now defaults to "list" below 768px and "calendar" at/above it, but only when localStorage has no stored preference yet (read via a lazy useState initializer); a useEffect persists every viewMode change to localStorage key "calendar_view_mode" so the choice survives reload/revisit. Toggle buttons unchanged.
- Had to update the existing e2e/calendar.spec.ts overflow tests: the 320/375/414px cases previously assumed calendar-grid was visible immediately on load, which broke once those widths started defaulting to list view — added an explicit click on view-toggle-calendar before the grid assertions at those widths. Added 3 new tests: mobile defaults to list, desktop defaults to calendar, and view choice persists across page.reload() (also asserted the literal localStorage value).
- Files changed: src/app/dashboard/calendar/page.tsx, e2e/calendar.spec.ts
- **Learnings for future iterations:**
  - When a story changes default UI state based on viewport/localStorage, check whether existing e2e tests assumed the old default — they may need a same-story update (not scope creep) rather than a separate story, since the test is directly verifying the changed behavior.
---

## 2026-06-16 - US-006
- Updated landing page copy in src/app/page.tsx: CtaButton label ("Build my free lawn plan →", shared component rendered in both the hero and bottom CTA section so one edit covers both), hero h1 ("A lawn-care plan for your grass and your location"), hero sub-copy, and bottom section heading ("Your plan is free and ready in 2 minutes.").
- Files changed: src/app/page.tsx
- **Learnings for future iterations:**
  - Landing page CTA text lives in a single shared CtaButton() component in src/app/page.tsx, reused for both the hero and the bottom-of-page CTA — copy edits there propagate to both locations automatically.
---

## 2026-06-16 - US-007
- Updated the FEATURES "Zone-aware" card description in src/app/page.tsx to "Plans calibrated to your USDA growing zone and based on university extension guidance for your region."
- grep -r "science-backed" src/ returned zero matches before starting this story — the only occurrence (hero sub-copy) had already been replaced as part of US-006's wording change in the prior story, so that acceptance criterion was already satisfied.
- Dashboard LOADING_MESSAGES "Consulting university extension research…" left as-is (already accurate per the story).
- Files changed: src/app/page.tsx
- **Learnings for future iterations:**
  - When picking up a copy-cleanup story, grep for the target string first — an earlier story's wording change may have already satisfied part of the acceptance criteria.
---

## 2026-06-16 - US-008
- Replaced the 3-item HOW_IT_WORKS array in src/app/page.tsx with the 4 onboarding-accurate steps (ZIP code, grass type, yard description, personalized plan). Rendering is a vertical ol/li list (not a fixed-column grid), so growing from 3 to 4 items needed no layout/markup changes.
- Files changed: src/app/page.tsx
- **Learnings for future iterations:**
  - HOW_IT_WORKS renders via .map() over a plain array with an inferred {step, title, description} shape — no separate type alias to update when changing item count or content.
---

## 2026-06-16 - US-009
- Replaced the lawn-size number-spinner input on the profile page with a plain text input (type="text", inputMode="numeric") that strips non-digit characters on change, per the workflow optimization for US-004+.
- Added inline validation: a non-empty value under 100 sq ft turns the border red and shows "Enter a lawn size of at least 100 sq ft" below the field.
- Updated the label to "Lawn Size (sq ft)" with a muted helper line underneath ("Used to calculate product quantities. Optional.") and added placeholder="e.g. 5,000".
- Files changed: src/app/profile/page.tsx, e2e/profile.spec.ts (new), prd.json, progress.txt. Screenshots: e2e/screenshots/profile-414x896.png, e2e/screenshots/profile-414x896-error.png.
- **Learnings for future iterations:**
  - No e2e coverage existed yet for `/profile` — added a small dedicated spec (e2e/profile.spec.ts) rather than folding profile assertions into an unrelated file. Mirrors the calendar/clerk-auth spec-per-route pattern.
  - The two `react-hooks/set-state-in-effect` lint errors in src/app/profile/page.tsx (lines 44 and 68) are pre-existing from US-001 and out of scope for every story touching this file — confirmed unaffected by this change, not fixed.
  - For text-input validation styling, reuse `var(--color-urgent)` for both the border and the error text color rather than introducing a new token.
---

## 2026-06-16 - US-010
- Standardized the profile page's sunlight options to show the same descriptive hour-range labels used during onboarding ("6+ hours direct sun", "3-6 hours", "Under 3 hours").
- Buttons changed from a single inline label to a stacked flex-col layout (label + desc), with desc text color flipping to var(--color-background) when selected for contrast against the unfilled-but-bordered selected state.
- Files changed: src/app/profile/page.tsx, e2e/profile.spec.ts, prd.json, progress.txt. Screenshot: e2e/screenshots/profile-sun-414x896.png.
- **Learnings for future iterations:**
  - When a screenshot looks visually tight/clipped at small scale, verify with a Playwright boundingBox() check before assuming a layout bug — in this case the description text fit comfortably and the screenshot just renders small.
  - profile.spec.ts is the right home for all /profile UI assertions going forward; keep adding test.describe blocks per field rather than new files.
---

## 2026-06-16 - US-011
- Added a prominent "None — I'm just getting started" option at the top of the onboarding treatment-history step (steps/Step5.tsx), replacing the bottom-anchored "None yet — it's early in the season" option.
- The button is now taller (py-4) and stacks the label with a "Skip ahead to get your plan" subtitle, visually distinct from the other py-3 task rows.
- Files changed: src/app/onboarding/[step]/steps/Step5.tsx, e2e/onboarding-step5.spec.ts (new), prd.json, progress.txt. Screenshot: e2e/screenshots/onboarding-step5-414x896.png.
- **Learnings for future iterations:**
  - steps/Step5New.tsx is a byte-for-byte duplicate of steps/Step5.tsx that nothing imports — page.tsx wires up steps/Step5.tsx (whose exported function happens to be misnamed `Step5New`). Future treatment-history stories (e.g. US-012) should edit steps/Step5.tsx only; don't waste time mirroring the dead duplicate file.
  - No onboarding e2e coverage existed before this story — added e2e/onboarding-step5.spec.ts as the first one, following the one-spec-per-route pattern from calendar/clerk-auth/profile.
---
