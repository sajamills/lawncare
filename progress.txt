## Codebase Patterns
- Next.js 15 project using App Router with `src/` directory layout (`src/app/`, `src/lib/`, `src/data/`, `src/actions/`)
- Tailwind CSS v4 uses `@import "tailwindcss"` in globals.css (not `@tailwind` directives)
- Tailwind CSS v4 custom colors: define via `@theme { --color-<name>: <value>; }` in globals.css; no tailwind.config.ts needed
- Use `var(--color-<name>)` in inline styles or CSS for semantic color tokens defined in `@theme`
- TypeScript strict mode enabled via tsconfig.json
- .gitignore blocks `.env*` — commit example env files as `env.local.example` (no leading dot)
- Clerk auth: clerkMiddleware + createRouteMatcher in src/middleware.ts; ClerkProvider wraps layout; server components use `auth()` from @clerk/nextjs/server

# Ralph Progress Log
Started: Sun Jun 14 10:56:13 CDT 2026
---

## 2026-06-14 - US-001
- Scaffolded Next.js 15 with App Router, TypeScript, Tailwind CSS, ESLint, src/ directory
- Installed: @clerk/nextjs, @vercel/postgres, ai, @anthropic-ai/sdk, next-pwa
- Cleared default placeholder content from src/app/page.tsx and src/app/globals.css
- Files changed: src/app/page.tsx, src/app/globals.css, package.json, package-lock.json, and all scaffold files
- **Learnings for future iterations:**
  - create-next-app refuses to run in a non-empty directory; scaffold in /tmp then rsync files over
  - Tailwind CSS v4 (bundled with Next.js 16 scaffold) uses `@import "tailwindcss"` syntax, not the v3 `@tailwind base/components/utilities` directives
  - next-env.d.ts is auto-generated by Next.js; do not hand-edit it
---

## 2026-06-14 - US-002
- Added `@theme` block in globals.css with all six design tokens: background, surface, primary, secondary, text-primary, text-muted
- Applied `background-color` and `color` to `body` via CSS variables in globals.css
- Updated layout.tsx with full-height dark shell, top nav bar showing 'LawnGuide' in green (#4caf50), metadata title updated
- Files changed: src/app/globals.css, src/app/layout.tsx
- **Learnings for future iterations:**
  - Tailwind v4 has no tailwind.config.ts; custom colors are defined via `@theme { --color-<name>: value; }` in globals.css
  - In Tailwind v4, `--color-text-primary` would map to class `text-text-primary` — awkward naming; prefer using CSS variables via `var(--color-*)` in inline styles for clarity
  - The `@theme` variables are also accessible in any CSS selector as standard CSS custom properties
---

## 2026-06-14 - US-003
- Created db/migrations/001_initial_schema.sql with user_profiles and cached_plans tables
- user_profiles: uuid pk, clerk_user_id (unique nullable), session_id (unique nullable), zip_code, state, usda_zone, grass_type, square_footage (nullable), has_pets (default false), sun_exposure, created_at, updated_at
- cached_plans: uuid pk, state, grass_type, pdf_url, parsed_plan (jsonb), created_at; UNIQUE constraint on (state, grass_type)
- pgcrypto extension enabled for gen_random_uuid()
- Files changed: db/migrations/001_initial_schema.sql
- **Learnings for future iterations:**
  - No .env.local exists; POSTGRES_URL must be set via `vercel env pull` before running migrations against a live DB
  - Use `CREATE EXTENSION IF NOT EXISTS pgcrypto` before any gen_random_uuid() calls in migrations
  - Use `CREATE TABLE IF NOT EXISTS` for idempotent migrations
---

## 2026-06-14 - US-004
- Created src/middleware.ts using clerkMiddleware() + createRouteMatcher to protect /dashboard and /profile routes
- Updated src/app/layout.tsx: wrapped in ClerkProvider, added NavAuth async server component showing SignInButton (signed out) or UserButton (signed in)
- Created env.local.example (not .env.local.example — gitignore blocks .env* pattern) with placeholder Clerk keys and Postgres URL
- Files changed: src/app/layout.tsx, src/middleware.ts, env.local.example
- **Learnings for future iterations:**
  - .gitignore uses `.env*` glob — use `env.local.example` (no leading dot) for committable example env files
  - @clerk/nextjs v7 UserButton no longer accepts `afterSignOutUrl` prop — omit it
  - NavAuth must be an async server component (uses `auth()` from @clerk/nextjs/server) — keep it separate from the layout function to avoid making the whole layout async
---

## 2026-06-14 - US-005
- Created src/lib/zip-zone-data.ts with ~700 ZIP prefix entries covering all 50 states + DC, mapping to state abbreviation and USDA hardiness zone
- Created src/app/api/zone/route.ts: GET ?zip=XXXXX returns { zip, state, stateName, zone }; 400 for non-5-digit, 404 for no match
- Files changed: src/lib/zip-zone-data.ts, src/app/api/zone/route.ts
- **Learnings for future iterations:**
  - ZIP prefix map uses 3-digit string keys (e.g. "275") — must use zip.slice(0,3) to look up
  - @/lib/* path alias works via tsconfig paths (already configured by create-next-app)
---

## 2026-06-14 - US-006
- Created src/data/grass-types.ts: 10 GrassType objects (Bermudagrass, Zoysia, St. Augustine, Centipede, Bahia, Kentucky Bluegrass, Tall Fescue, Fine Fescue, Perennial Ryegrass, Buffalo Grass)
- Created src/data/quiz-questions.ts: 5 QuizQuestion objects covering blade width, texture, color, growth pattern, active season
- Created src/lib/grass-scorer.ts: scoreGrassTypes(answers) returns ScoredGrassType[] sorted by match count descending
- Files changed: src/data/grass-types.ts, src/data/quiz-questions.ts, src/lib/grass-scorer.ts
- **Learnings for future iterations:**
  - grass-scorer.ts uses Record<string, string> answers where keys are the trait field names on GrassType (bladeWidth, texture, color, growthPattern, activeSeason)
  - QuizQuestion options use `trait` (field name) and `traitValue` (field value) for the scorer to match against
---

## 2026-06-14 - US-007
- Created src/data/extension-sources.ts: static map of all 50 states + DC to land-grant university extension info; knownPdfUrls map for common state+grassType combos
- Created src/app/api/find-pdf/route.ts: POST { state, grassType } → returns { pdfUrl, source } from known URLs or constructs search URL as fallback (with fallback: true flag)
- Files changed: src/data/extension-sources.ts, src/app/api/find-pdf/route.ts
- **Learnings for future iterations:**
  - Route returns a search URL (not a direct PDF URL) as fallback — parse-pdf route must handle non-PDF URLs gracefully
  - knownPdfUrls key format is `${STATE}_${grassType-id}` (e.g. "NC_tall-fescue")
---

## 2026-06-14 - US-008
- Created src/app/api/parse-pdf/route.ts: POST { pdfUrl, state, grassType }
- Checks cached_plans DB first; fetches URL as PDF buffer or HTML text; calls claude-sonnet-4-6 with document or text content
- Extracts JSON array from Claude response via regex; caches result to DB; gracefully degrades if DB unavailable
- Files changed: src/app/api/parse-pdf/route.ts
- **Learnings for future iterations:**
  - Anthropic SDK document type for PDFs uses base64-encoded buffer with media_type "application/pdf"
  - Claude response may wrap JSON in markdown — use regex `\[[\s\S]*\]` to extract the array
  - DB operations are wrapped in try/catch so the route still works in dev without POSTGRES_URL
---

## 2026-06-14 - US-009
- Created src/actions/profile.ts with "use server" directive
- saveProfile(): upserts user_profiles on session_id conflict, sets clerk_user_id if signed in; returns { success, error? }
- getProfile(): queries by clerk_user_id OR session_id, returns most recent record; returns null if not found or DB unavailable
- ProfileData type exported for use by UI components
- Files changed: src/actions/profile.ts
- **Learnings for future iterations:**
  - Server actions must start with "use server" directive
  - ON CONFLICT (session_id) requires session_id to have UNIQUE constraint — already defined in migration
  - getProfile gracefully returns null when DB is unavailable (for dev without POSTGRES_URL)
---

## 2026-06-14 - US-010
- Created src/app/onboarding/layout.tsx: "use client" layout with 5-segment progress bar; active=green, completed=dimmed green, future=dark
- Created src/app/onboarding/page.tsx: redirects to /onboarding/1
- Created src/app/onboarding/[step]/page.tsx: routes to StepN component, renders Back button on steps 2-5
- Created placeholder step components Step1-Step5 in src/app/onboarding/[step]/steps/
- Files changed: src/app/onboarding/{layout.tsx, page.tsx, [step]/page.tsx, [step]/steps/Step1-5.tsx}
- **Learnings for future iterations:**
  - onboarding/layout.tsx must be "use client" to use usePathname() for active step detection
  - Step components receive onNext prop from [step]/page.tsx for navigation
  - Step placeholder files will be replaced by US-011 through US-015
  - sessionStorage persistence of onboarding_state happens within each step component
---

## 2026-06-14 - US-011
- Replaced Step1.tsx placeholder with full ZIP code entry form
- ZIP input: numeric only, 5 digits max, calls /api/zone on submit
- On success: shows "Zone 7a — North Carolina" in green and reveals Continue button
- On error: shows error message in red
- onNext() saves { zip, state, stateName, usda_zone } to sessionStorage onboarding_state
- Files changed: src/app/onboarding/[step]/steps/Step1.tsx
- **Learnings for future iterations:**
  - getOnboardingState/setOnboardingState helper functions manage sessionStorage — reuse pattern in Steps 2-5
  - Step components use "use client" directive and receive onNext from [step]/page.tsx
---

## 2026-06-14 - US-012
- Replaced Step2.tsx with full yard profile inputs: sq footage + "not sure" toggle, pets Yes/No, sun exposure 3-card select
- Continue enabled only when pets and sun exposure selected (sq ft optional)
- Saves { square_footage, has_pets, sun_exposure } to sessionStorage onboarding_state on Continue
- Files changed: src/app/onboarding/[step]/steps/Step2.tsx
---

## 2026-06-14 - US-013
- Replaced Step3.tsx with one-at-a-time quiz using quizQuestions data
- Selecting answer immediately advances to next question (no Next button)
- After last question: calls scoreGrassTypes(), saves { quiz_answers, grass_scores } to sessionStorage, calls onNext()
- Skip link navigates directly to /onboarding/4 (no quiz answers saved)
- Files changed: src/app/onboarding/[step]/steps/Step3.tsx
---

## 2026-06-14 - US-014
- Replaced Step4.tsx with grass type selection showing top 2-3 quiz results or full list
- Card selection with green border highlight; "This is my grass" confirm button appears after selection
- "None of these match" link expands to all 10 grass types
- Trait chips show blade width, growth pattern, active season
- Saves { grass_type: grass.id } to sessionStorage on confirm
- Files changed: src/app/onboarding/[step]/steps/Step4.tsx
---

## 2026-06-14 - US-015
- Replaced Step5.tsx with plan generation flow: spinner while loading, done state with Sign Up + Continue options
- On mount: calls saveProfile() server action, then /api/find-pdf, then /api/parse-pdf in sequence
- Both API calls are best-effort (try/catch) — navigates to dashboard regardless
- Uses useRef(started) to prevent double-execution in React StrictMode
- getOrCreateSessionId() creates UUID in localStorage under 'lawn_session_id'
- Files changed: src/app/onboarding/[step]/steps/Step5.tsx
---

## 2026-06-14 - US-016
- Created src/app/dashboard/page.tsx: "Do This Now" view loading current month tasks
- Loads cached plan via POST /api/parse-pdf (returns cached version without re-parsing)
- Tasks sorted urgent→routine→optional; category icons + priority badges
- Pet caution banner shown when hasPets and task has petSafetyNote
- scaleQuantity() replaces "X lbs per 1,000 sq ft" with scaled total
- Empty state shown when no tasks for current month
- Files changed: src/app/dashboard/page.tsx
---

## 2026-06-14 - US-017
- Created src/app/dashboard/calendar/page.tsx: annual calendar with Calendar/List toggle
- Calendar view: 12 month columns, horizontal scroll, color dot chips per category, click to expand month
- List view: all 12 months stacked with full task cards
- Current month highlighted with green border/color
- Legend at bottom shows all 7 category colors
- Files changed: src/app/dashboard/calendar/page.tsx
---

## 2026-06-14 - US-018
- Created src/app/sign-in/[[...sign-in]]/page.tsx and src/app/sign-up/[[...sign-up]]/page.tsx
- Both pages center Clerk SignIn/SignUp components with dark theme appearance (colorBackground, colorPrimary)
- LawnGuide name shown above each form in green
- Clerk appearance.variables supports: colorBackground, colorPrimary, colorDanger, colorSuccess, colorWarning — NOT colorText/colorInputBackground in v7
- Files changed: src/app/sign-in/[[...sign-in]]/page.tsx, src/app/sign-up/[[...sign-up]]/page.tsx
- **Learnings for future iterations:**
  - @clerk/nextjs v7 appearance.variables only supports a limited set: colorBackground, colorPrimary, colorDanger, colorSuccess, colorWarning, fontFamily, borderRadius — check types before adding
---
